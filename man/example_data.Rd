% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example_data.R
\name{example_data}
\alias{example_data}
\title{Load NCRNWater example WQP data & metadata}
\usage{
example_data(
  network = "NCRN",
  reader = c("readr", "utils"),
  as_tibble = TRUE,
  assign = FALSE,
  envir = .GlobalEnv,
  data_name = "wqp",
  metadata_name = "wqp_metadata"
)
}
\arguments{
\item{network}{Character; default "NCRN".}

\item{reader}{Which reader to use: \code{"readr"} (default) or \code{"utils"}.}

\item{as_tibble}{Logical; when \code{TRUE} and \code{reader="readr"}, returns tibbles.}

\item{assign}{Logical; when \code{TRUE}, assigns the loaded objects into \code{envir}.}

\item{envir}{Environment; target for assignment when \code{assign=TRUE}. Default \code{.GlobalEnv}.}

\item{data_name}{Name to assign for the WQP data (default \code{"wqp"}).}

\item{metadata_name}{Name to assign for the metadata (default \code{"wqp_metadata"}).}
}
\value{
A list with elements \code{wqp} and \code{wqp_metadata}.
}
\description{
Reads the example CSVs installed with the package (under \code{inst/extdata/NCRN/})
and returns them as tibbles (or \code{data.frame}s). Optionally assigns them into
an environment (e.g., the user's global environment) under convenient names.
}
\examples{
# Return in-memory tibbles (no assignment)
ex <- example_data(assign = FALSE)
dplyr::glimpse(ex$wqp)

# Assign to global env as 'wqp' and 'wqp_metadata'
use_example_data(assign = TRUE)
ls()  # should show 'wqp' and 'wqp_metadata'

}
