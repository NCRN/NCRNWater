% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filterActive.R
\name{filterActive}
\alias{filterActive}
\title{Filter metadata & data files down to active logical keys}
\usage{
filterActive(
  network,
  metadata_filename,
  data_filename,
  dir = "Data",
  wqx = TRUE,
  out_dir = NULL
)
}
\arguments{
\item{network}{Character, e.g., "NCRN". Used to build input directory: file.path(dir, network).}

\item{metadata_filename}{The metadata CSV filename, e.g., "wqp_ncrnwater_metadata.csv".}

\item{data_filename}{The data CSV filename, e.g., "wqp.csv".}

\item{dir}{Base directory containing your data folders; default "Data".}

\item{wqx}{Logical; TRUE if the data file is WQX format.}

\item{out_dir}{Output directory for filtered files; default is the same folder as inputs.}
}
\value{
A list with:
  \describe{
    \item{mname_active}{Filtered metadata filename (basename).}
    \item{dname_active}{Filtered data filename (basename).}
    \item{meta_path}{Full path to filtered metadata.}
    \item{data_path}{Full path to filtered data.}
    \item{dropped}{A data.frame of dropped metadata rows (for diagnostics).}
  }
}
\description{
Reads raw metadata & data CSVs, filters to "active" logical keys, normalizes
comparator strings, and writes filtered copies to disk. Returns filenames
suitable for passing to `importNCRNWater()`.
}
